### 4.7 星期二

尝试安装docker，windows系统太老无法安装运行
查看B站视频，收获不大
根据视频下载ollama，安装完成后继续安装模型，不好用放弃
根据视频通过langchain.js链接本地ollama，体验差，放弃

### 4.8 星期三

申请腾讯混元key，需要用腾讯控制台，连接需要name，控制台只找到key，傻福
申请阿里百炼，langchain.js试了几次不好使，放弃
总结langchain.js用的人少，相关文档少，体验差，改用langchain
发现deepseek在langchain支持的供应商列表中，改用deepseek
langchain链接deepseek成功
express+mongodb搭建后台

### 4.9 星期四

vue-cli生成vue3+ts+less前端项目，ts各种报错，傻福，不是为了学ts建议别用
通过元宝生成登陆页面，体验非常棒,太快了

### 4.10 星期五

完善登陆页面，开发登陆请求，引用axios后前端页面报错，无法解决无奈降低axios版本
后台使用mongoose连接mongodb，查不到数，各种问元宝无法解决，改问deepseek解决，mongoose就是傻福，集合名自动变复数，大傻福
通过注册接口创建用户，deepseek生成会话管理代码，会话信息存mongodb持久化
登陆成功后页面跳转，接口401跳转登陆页面
总结deepseek给的代码比元宝质量高一点，细节好些
开始开发对话页面，使用element-plus-x

### 4.13 星期一

参考session的Schema，实现了chat中存储historyid
发起对话，使用langchain.js调用deepseek，没用python，链路太长了，存储会话、对话完成
卡在流式响应，前端一直接收到一整坨，最后绕过前端代理跨域连接服务端正常，确定是代理问题，ai没有给出解决方案，放弃处理，生产也不会用devserver代理
整理代码，删除调试加的乱七八糟

### 4.14 星期二

多轮对话bug修复，同时监视sse开始时返回的会话id和对话id，刷新对话列表
开启新对话按钮，历史会话列表样式优化，前端代码优化，vue3setup非常容易把代码写的非常混乱
ai聊天内容改为md
实现了会话持久化(存储到mongodb)多轮对话(没有用内存，直接mongodb读取)
直接将express路由文件扔给deepseek，提出`我的代码已经自行实现了对话持久化，但历史会话并没用用到新会话中，使用langchain.js进行优化，将会话中的关键信息（如用户偏好、实体事实、对话摘要）保存到数据库、文件或向量存储中，下次会话可加载使用。`
根据ai给的答案，对历史会话进行摘要存储，没有用向量库实现检索式记忆，继续提问`你的回答提到安装chromadb，但后续给到的代码修改没用用到向量存储，而是直接使用mongodb进行了存储，能否优化代码，使用向量存储`
向量提取遇到问题，DeepSeek 不提供 Embedding API，最方便的是OpenAI ，不想翻墙，转用传统算法（可代码直接实现）提取向量
失败，报错deepseek解决不了，可能是条错误路线
~~TODO：暂时放弃向量检索，回退到对历史会话进行摘要存储~~
解决流式渲染漏字问题

### 4.15 星期三

优化对话气泡区域样式
升级web项目ts版本，消除报错提示
角色、风格固定导致不相关问题无法得到理想回答且容易污染长期记忆，解决方案添加快速意图分析
mongodb中添加测试业务数据，创建元数据集合，记录每个业务集合的元数据，包括集合名、集合描述、字段名、字段描述、字段格式、字段示例
发现将元数据全部塞给模型太多了，解决方案还是向量提取
chromadb官网发现不用docker部署适用nodejs的方案，deepseek没有向量模型，选用Qwen3-Embedding作为向量模型
Qwen3-Embedding代码示例curl版本调用失败，根据此版本生成的js代码同样不行，根据python版本使用openai调用成功

### 4.16 星期四

[chroma下载地址](https://github.com/chroma-core/chroma/releases)
下载完成后不需要安装，参考[官网手册](https://docs.trychroma.com/docs/overview/getting-started#typescript)运行即可
chroma报错`Cannot instantiate a collection with the DefaultEmbeddingFunction. Please install @chroma-core/default-embed, or provide a different embedding function`好像没影响，deepseek没能解决，暂不处理
新增meta页面，可编辑元数据，编辑成功后点击按钮提取向量
元数据提取向量时组织的文本包含每个字段的例子，导致向量误匹配，删除举例，仍然不行，暂时放弃

添加类型选择节省token，未选择类型时，通过问题模板向量检索判断用户想要聊天还是查询业务数据

### 4.17 星期五

代码增多后进度明显慢了，一个是没法直接扔个ai了，另一个是乱七八糟上了很多东西太杂乱了
向量匹配的问题想明白了，向量匹配不管差距多大一定会按排名返回，需要自己根据distances取舍
集合元数集合、问题模板集合 改为自动提取向量
DONE：未选择类型 维护问题模板向量查询判断用户意图
DONE：业务查询流程：向量查询对应的集合、模型生成查询语句、执行查询语句、查询结果和问题交给模型分析、分析结果返回
TOOD：未查询到对应集合处理
DONE：问题模板记录，长期记忆新增按照角色区分
TODO: 匹配多张表，实现关联查询
~~TODO：业务数据查询未添加长期记忆~~ 不加了生成查询语句和分析查询数据都不需要长期记忆
